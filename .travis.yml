language: python

stages:
- test
- name: deploy
  if: tag =~ tdvt

script:
- python -m unittest

jobs:
  include:
  - python: 3.6
  - stage: deploy
    script:
    - npm install -g jfrog-cli-go
    - jfrog rt p --url=$PROD_URL --apikey=$PROD_API_KEY
